interface:
  display_name: "Research Empirics"
  short_description: "Build, diagnose, and run empirical workflow"
  default_prompt: "Use $research-empirics to build the sample, run visual diagnostics, estimate results, and update the run log."
