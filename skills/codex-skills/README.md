# Codex Skills

Small, reusable Codex utility skills live here.

Each utility should be self-contained under its own subfolder, with a `SKILL.md` at the skill root and any helper scripts under `scripts/`.

## Included

- `codex-usage/`: reads the latest local Codex rate-limit snapshot and reports remaining quota/reset information without exposing chat content or credentials.
